(()=>{const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];const root=document.documentElement;const saved=localStorage.getItem('gtos-theme');if(saved)root.dataset.theme=saved;else if(matchMedia('(prefers-color-scheme:dark)').matches)root.dataset.theme='dark';function toggleTheme(){root.dataset.theme=root.dataset.theme==='dark'?'light':'dark';localStorage.setItem('gtos-theme',root.dataset.theme)}$$('.theme-toggle').forEach(b=>b.addEventListener('click',toggleTheme));$('.mobile-nav-toggle')?.addEventListener('click',()=>document.body.classList.add('nav-open'));$('.sidebar-close')?.addEventListener('click',()=>document.body.classList.remove('nav-open'));
function makeTree(node){const ul=document.createElement('ul');for(const child of node.children||[]){const li=document.createElement('li');const d=document.createElement('details');const summary=document.createElement('summary');summary.textContent=child.name;d.append(summary);d.append(makeTree(child));li.append(d);ul.append(li)}for(const p of node.pages||[]){const li=document.createElement('li'),a=document.createElement('a');a.href=relHref(p.href);a.textContent=p.title;a.dataset.search=p.title.toLowerCase();if(location.pathname.endsWith(p.href)||document.body.dataset.current===p.href){a.classList.add('active');let n=li.parentElement;setTimeout(()=>{let x=a.parentElement;while(x){if(x.tagName==='DETAILS')x.open=true;x=x.parentElement}},0)}li.append(a);ul.append(li)}return ul}function relHref(abs){const target=abs.replace(/^\//,'');const base=document.body.dataset.root||'';return base+target}const nav=$('#handbook-nav');if(nav&&window.GTOS_NAV)nav.append(makeTree(window.GTOS_NAV));const filter=$('.nav-filter');filter?.addEventListener('input',()=>{const q=filter.value.toLowerCase().trim();$$('#handbook-nav a').forEach(a=>{a.parentElement.style.display=!q||a.dataset.search.includes(q)?'':'none'});$$('#handbook-nav details').forEach(d=>{if(q)d.open=true})});
const modal=$('.search-modal'),searchInput=$('.search-modal input'),results=$('.search-results'),meta=$('.search-meta');function openSearch(){modal?.classList.add('open');modal?.setAttribute('aria-hidden','false');setTimeout(()=>searchInput?.focus(),40)}function closeSearch(){modal?.classList.remove('open');modal?.setAttribute('aria-hidden','true')}$$('.search-trigger').forEach(b=>b.addEventListener('click',openSearch));$('.search-close')?.addEventListener('click',closeSearch);modal?.addEventListener('click',e=>{if(e.target===modal)closeSearch()});document.addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();openSearch()}else if(e.key==='/'&&!/input|textarea/i.test(document.activeElement.tagName)){e.preventDefault();openSearch()}else if(e.key==='Escape'){closeSearch();closeDiagram()}});function esc(s){return String(s).replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]))}function highlightText(s,terms){let out=esc(s);for(const t of terms){if(t.length<2)continue;out=out.replace(new RegExp('('+t.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')+')','ig'),'<mark>$1</mark>')}return out}let timer;searchInput?.addEventListener('input',()=>{clearTimeout(timer);timer=setTimeout(runSearch,80)});function runSearch(){const q=searchInput.value.trim().toLowerCase(),terms=q.split(/\s+/).filter(Boolean);if(!q){results.innerHTML='';meta.textContent='Type to search across the complete handbook.';return}const scored=[];for(const x of window.GTOS_SEARCH||[]){const title=x.title.toLowerCase(),heads=x.headings.toLowerCase(),body=x.text.toLowerCase(),section=x.section.toLowerCase();let score=0,ok=true;for(const t of terms){const found=title.includes(t)||heads.includes(t)||body.includes(t)||section.includes(t);if(!found){ok=false;break}score+=(title.includes(t)?12:0)+(heads.includes(t)?6:0)+(section.includes(t)?3:0)+Math.min(4,(body.split(t).length-1))}if(ok)scored.push([score,x])}scored.sort((a,b)=>b[0]-a[0]);const top=scored.slice(0,60);meta.textContent=`${scored.length} matching pages`;results.innerHTML=top.map(([,x])=>{let snippet=x.text;const first=Math.min(...terms.map(t=>{const i=snippet.toLowerCase().indexOf(t);return i<0?999999:i}));if(first<999999)snippet=snippet.slice(Math.max(0,first-90),first+250);else snippet=snippet.slice(0,340);return `<a class="search-result" href="${relHref(x.href)}"><span>${esc(x.section)}</span><strong>${highlightText(x.title,terms)}</strong><p>${highlightText(snippet,terms)}</p></a>`}).join('')||'<p style="padding:20px;color:var(--muted)">No results. Try broader architecture or Domain terms.</p>'}
$$('.copy-code').forEach(b=>b.addEventListener('click',async()=>{const code=b.closest('.code-block')?.querySelector('code')?.innerText||'';await navigator.clipboard.writeText(code);const old=b.textContent;b.textContent='Copied';setTimeout(()=>b.textContent=old,1200)}));$('.print-page')?.addEventListener('click',()=>print());
const dm=$('.diagram-modal'),dmc=$('.diagram-modal-content');function closeDiagram(){dm?.classList.remove('open');dm?.setAttribute('aria-hidden','true');if(dmc)dmc.innerHTML=''}$$('.diagram-expand,.diagram-card>img').forEach(el=>el.addEventListener('click',()=>{const card=el.closest('.diagram-card'),img=card?.querySelector('img');if(!img||!dm||!dmc)return;dmc.innerHTML=`<img src="${img.src}" alt="${esc(img.alt)}">`;dm.classList.add('open');dm.setAttribute('aria-hidden','false')}));$('.diagram-modal-close')?.addEventListener('click',closeDiagram);dm?.addEventListener('click',e=>{if(e.target===dm)closeDiagram()});
const progress=$('.reading-progress span');function onScroll(){if(progress){const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=(max>0?scrollY/max*100:0)+'%'}}addEventListener('scroll',onScroll,{passive:true});onScroll();const tocLinks=$$('.page-toc a');if(tocLinks.length){const map=new Map(tocLinks.map(a=>[a.getAttribute('href').slice(1),a]));const obs=new IntersectionObserver(entries=>{for(const e of entries){if(e.isIntersecting){tocLinks.forEach(a=>a.classList.remove('active'));map.get(e.target.id)?.classList.add('active')}}},{rootMargin:'-20% 0px -70%'});for(const id of map.keys()){const el=document.getElementById(id);if(el)obs.observe(el)}}
if('serviceWorker'in navigator&&location.protocol.startsWith('http'))navigator.serviceWorker.register(relHref('/service-worker.js')).catch(()=>{});})();